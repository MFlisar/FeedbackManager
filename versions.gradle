ext {

    setup = [
            compileSdk       : 32,
            enableDataBinding: false,
            enableViewBinding: true,
            minSdk           : 16,
            targetSdk        : 32
    ]

    versions = [
            kotlinVersion    : "1.7.10",
            gradlePlugin     : "7.2.1",
            mavenVersion     : "2.0",

            core             : "1.8.0",

            cacheFileProvider: "0.2"
    ]

    debugSetup = [useMultiDex: false]
    releaseSetup = [useMultiDex: false]
}